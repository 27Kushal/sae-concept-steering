# ==============================================================================
# Requirements for "Interpretable Concept Steering via Sparse Autoencoders"
# ==============================================================================
#
# LOCAL (Apple Silicon Mac M4 / MPS / CPU):
#   Run: pip install -r requirements.txt
#   Note: PyTorch from standard PyPI includes native Apple Silicon MPS acceleration.
#   Export: export PYTORCH_ENABLE_MPS_FALLBACK=1
#
# GOOGLE COLAB (T4 GPU / CUDA):
#   Run: pip install -r requirements.txt
#   PyTorch with CUDA is pre-installed in Colab runtime.
# ==============================================================================

# Core Deep Learning
torch>=2.0.0
transformers>=4.38.0
transformer_lens>=1.14.0
datasets>=2.16.0
accelerate>=0.26.0
einops>=0.7.0
jaxtyping>=0.2.25

# Scientific Computing & Evaluation
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
pandas>=2.0.0
tqdm>=4.66.0
